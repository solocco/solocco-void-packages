name: Build Packages
on:
  push:
    branches:
      - main
    paths:
      - 'srcpkgs/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-all:
    name: Build All Packages x86_64
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/void-linux/void-glibc-full
      options: --privileged
      volumes:
        - /dev:/dev
      env:
        ARCH: x86_64
        BOOTSTRAP: x86_64
        TEST: 0

    steps:
      - name: Prepare container
        run: |
          mkdir -p /etc/xbps.d && cp /usr/share/xbps.d/*-repository-*.conf /etc/xbps.d/
          sed -i 's|repo-default|repo-ci|g' /etc/xbps.d/*-repository-*.conf
          xbps-install -Syu xbps && xbps-install -yu && xbps-install -y sudo bash curl git
          useradd -G xbuilder -M builder
          echo "builder ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers.d/builder

      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          path: main-repo

      - name: Checkout void-packages
        uses: actions/checkout@v4
        with:
          repository: void-linux/void-packages
          path: void-packages

      - name: Merge templates
        id: info
        run: |
          cp -rv main-repo/srcpkgs/zen-browser-bin void-packages/srcpkgs/
          cp -rv main-repo/srcpkgs/google-chrome-bin void-packages/srcpkgs/
          chown -R builder:builder void-packages
          ZEN_VER=$(grep '^version=' main-repo/srcpkgs/zen-browser-bin/template | cut -d= -f2 | tr -d '"')
          CHR_VER=$(grep '^version=' main-repo/srcpkgs/google-chrome-bin/template | cut -d= -f2 | tr -d '"')
          echo "zen_version=$ZEN_VER" >> $GITHUB_OUTPUT
          echo "chrome_version=$CHR_VER" >> $GITHUB_OUTPUT

      - name: Prepare masterdir
        run: |
          cd void-packages
          sudo -Eu builder common/travis/set_mirror.sh
          sudo -Eu builder common/travis/prepare.sh
          common/travis/fetch-xtools.sh

      - name: Build zen-browser-bin
        run: |
          export PATH="/opt/xbps/usr/bin/:$PATH"
          cd void-packages
          sudo -Eu builder ./xbps-src -j$(nproc) pkg zen-browser-bin

      - name: Build google-chrome-bin
        run: |
          export PATH="/opt/xbps/usr/bin/:$PATH"
          cd void-packages
          mkdir -p common/conf
          echo "XBPS_ALLOW_RESTRICTED=yes" | sudo tee -a common/conf/make.conf
          sudo -Eu builder ./xbps-src -j$(nproc) pkg google-chrome-bin

      - name: Collect all packages
        run: |
          mkdir -p /tmp/repo
          find void-packages/hostdir/binpkgs -name "*.xbps" -exec cp {} /tmp/repo/ \;
          find void-packages/hostdir/binpkgs -name "*.xbps.sig2" -exec cp {} /tmp/repo/ \;

      - name: Sign and Index
        env:
          PRIV_KEY: ${{ secrets.PRIV_KEY }}
        run: |
          export PATH="/opt/xbps/usr/bin/:$PATH"
          cd /tmp/repo
          echo "$PRIV_KEY" > private.pem
          xbps-rindex -a *.xbps
          xbps-rindex -s --signedby "solocco" --privkey private.pem $PWD
          xbps-rindex -S --privkey private.pem *.xbps
          rm private.pem

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: repo-v${{ github.run_number }}
          name: "solocco-void-packages (zen: ${{ steps.info.outputs.zen_version }}, chrome: ${{ steps.info.outputs.chrome_version }})"
          body: |
            Automated build of all packages for Void Linux.

            | Package | Version |
            |---|---|
            | zen-browser-bin | ${{ steps.info.outputs.zen_version }} |
            | google-chrome-bin | ${{ steps.info.outputs.chrome_version }} |

            Architecture: x86_64 (glibc)
          make_latest: true
          files: /tmp/repo/*
